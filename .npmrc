# Next.js resolves React through its own server renderer. Under pnpm's default strict
# linking, prerendering finds a second React instance and fails with a null dispatcher.
# Hoisted linking keeps one React instance across the workspace. See ADR 0007.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
