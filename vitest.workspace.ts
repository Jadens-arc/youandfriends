/**
 * Vitest workspace.
 *
 * Task `001` adds jsdom environments, Testing Library setup, and coverage thresholds.
 * Today this simply ensures every package's tests are discovered and run.
 */
export default ['packages/*', 'apps/web'];
