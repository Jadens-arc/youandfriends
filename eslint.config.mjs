// Root ESLint config. The shared rules live in `packages/config` so every workspace,
// and this root, resolve the same set — see task `001`.

import config from '@youandfriends/config/eslint';

export default config;
